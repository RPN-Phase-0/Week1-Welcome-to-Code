for (let i = 1; i <= 100; i++) { // buat perulangan 1-100
    if (i % 2 === 0) { // jika i % 2 = 0, tampilkan genap
        console.log(`${i} - Genap`);
    } else { // jika i % 2 != 0, tampilkan ganjil
        console.log(`${i} - Ganjil`);
    }
}