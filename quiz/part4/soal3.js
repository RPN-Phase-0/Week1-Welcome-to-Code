function processSentence(nama, umur, alamat, hobi){
    return `Nama saya ${nama} umur saya ${umur} alamat saya di ${alamat} dan saya punya hobi ${hobi}`;
};

let data = processSentence( "jk", "20", "jln.random rpn ", " gitaran");

console.log(data)
